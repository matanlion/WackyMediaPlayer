/* Represents the root application controller */

import Ember from 'ember';

export default Ember.Controller.extend({	
	notificationContent: '',	//user notification content 
	isNotificationSuccess: '',	//user notification type
});
